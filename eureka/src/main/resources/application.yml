spring:
  application:
    name: eureka-server # Replace with the name of your service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true